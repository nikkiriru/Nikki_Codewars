// It's pretty straightforward. Your goal is to create a function that removes the first and last characters of a string. You're given one parameter, the original string. 

func removeFirstAndLast(_ str: String) -> String {
    var changeableString = str
    guard changeableString.count >= 3 else {return ""}
    changeableString.removeFirst()
    changeableString.removeLast()
    return changeableString
}
